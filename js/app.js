/**
 * Main App Entry
 * Imports and initializes all modules
 */

// We are using classic script tags in HTML rather than modules for maximum browser compatibility without a bundler.
// The scripts will be loaded in this order in HTML:
// 1. theme.js
// 2. password.js
// 3. validation.js
// 4. animations.js
// 5. app.js

document.addEventListener('DOMContentLoaded', () => {
    console.log("Premium Auth UI Kit Initialized");
});
